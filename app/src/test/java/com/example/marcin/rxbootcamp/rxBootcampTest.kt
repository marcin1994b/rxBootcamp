package com.example.marcin.rxbootcamp


import org.junit.Test

/**
 * Created by Marcin on 20.09.2017.
 */
class rxBootcampTest {

    @Test
    fun rxTest(){
       /* val testObserver = Observable.just("Aaa Bbb", "Ccc Ddd")
                .flatMapIterable{ it.split("\\s+")}
                .map { it.substring(0,1)
                        it.plus(".")}
                .buffer(2)
                .doOnNext { println(it.toString()) }*/

        /*
        fun initialSingleton(String) : Single<String>
        Single.just(name)
                map(it.split(" ")
                toObservable
                flatMapIterable(it)
                filter(it.isNotBlank)
                map(it[0])
                map(it.toUpperCase())
                .toList()
                map { it.joinToString(separator, postfix)}*/



    }

}